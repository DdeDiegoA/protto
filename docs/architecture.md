# Architecture

> Project: protto | Stack: bash | Generated by protto on 2026-07-27.

## Overview
Start with one paragraph describing what protto does and who uses it.

## Build & Test
- Build: `bash -n protto && bash -n lib/*.sh`
- Test: `./tests/test.bash`
- Lint: `echo 'TODO: add lint command'`

## Structure
- protto — main CLI entrypoint
- lib/ — shell libraries
- tests/ — test suites
- README.md — project overview
- docs/ — documentation
- .claude/ / .opencode/ — agent configuration

## Entrypoints
List the main entrypoints and how data flows between them.

## Decisions
See `docs/decisions.md`.

## Context
See `docs/context.md`.
